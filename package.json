{
  "name": "event-management-blockchain",
  "version": "1.0.0",
  "description": "Blockchain-based Event Management System",
  "scripts": {
    "setup": "npm install && cd client && npm install && cd ../server && npm install && cd ../blockchain && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run blockchain\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "blockchain": "cd blockchain && npm start",
    "build": "cd client && npm run build",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd server && npm test",
    "test:client": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
